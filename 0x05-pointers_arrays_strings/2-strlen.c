#include <string.h>
#include "main.h"

/**
 * _strlen - Function that returns the length of a string
 * @s: *s is a pointer to a variable of type char
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
